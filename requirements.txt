# Runtime dependencies. Pinned to versions this project is verified against.
# Install: pip install -r requirements.txt
#
# NOTE: torch is pinned to the CPU wheel. For GPU, install torch separately
# from https://pytorch.org before running this file.

# ---- web ----
fastapi==0.119.0
uvicorn[standard]==0.37.0
pydantic==2.12.2
pydantic-settings==2.11.0
python-multipart==0.0.20

# ---- auth ----
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.5.0
email-validator==2.3.0

# ---- storage / rate limiting ----
SQLAlchemy==2.0.44
redis==7.0.0
fastapi-limiter==0.1.6

# ---- retrieval (these were missing entirely: the Docker image could not boot) ----
faiss-cpu==1.12.0
sentence-transformers==3.0.1
transformers==4.57.1
torch==2.3.1
numpy==1.26.4
scipy==1.16.2
scikit-learn==1.7.2

# ---- scraping ----
requests==2.32.5
beautifulsoup4==4.14.2

# ---- misc ----
tqdm==4.67.1
language-tool-python==2.9.4
